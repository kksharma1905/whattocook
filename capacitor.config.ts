import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.whattocook.app",
  appName: "WhatToCook",
  webDir: "out",
  android: {
    backgroundColor: "#f4efe6",
  },
};

export default config;
